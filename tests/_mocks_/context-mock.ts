import { Filter, Pagination } from '@src/interfaces/common.interface';

export const mockSocketId: string = 'mock-socket-id';

export const mockDefaultPagination: Pagination = {
  page: 0,
  pageSize: 20,
};

export const mockFilter: Filter = {
  search: 'mock-search',
};
